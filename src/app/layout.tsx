import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { AuthProvider } from "@/contexts/auth-context";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "IT Asset Management - Coffee Shop Stores",
  description: "Comprehensive IT asset management system for coffee shop stores across Indonesia",
  keywords: ["IT Asset Management", "Coffee Shop", "Inventory", "Device Tracking", "Indonesia"],
  authors: [{ name: "IT Asset Management Team" }],
  icons: {
    icon: "/favicon.ico",
  },
  openGraph: {
    title: "IT Asset Management System",
    description: "Manage IT devices across 900+ coffee shop stores",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <AuthProvider>
          {children}
          <Toaster />
        </AuthProvider>
      </body>
    </html>
  );
}
